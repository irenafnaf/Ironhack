<h3><%= @post.title %> </h3>
				
		<img src ="<%= @post.gif_url %>">
		<br>
		<br>

		<form action = "/posts/<%= @post.id%>/upvote" method="POST"> <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
			<button type="submit"> Vote Here! </button>
		</form>
		<br>

	<%= "Total Votes: #{@post.nov}" %>
	<br>
	<br>

	<%= link_to("Create NEW", posts_new_path) %>
	<br>
	<br>


	<%= link_to("Back to All", posts_path) %>
