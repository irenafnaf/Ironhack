<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br>
	<%= f.label :gif_url %>
	<%= f.text_field :gif_url %>
	<br>

	<%= f.submit "Post GIF" %>
<% end %>

<%= link_to("Back to All", posts_path) %>